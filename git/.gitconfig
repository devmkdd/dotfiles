[include]
	# local config to include machine specific settings like account
	path = ~/.gitconfig.local
	path = ~/.config/delta/catppuccin.gitconfig

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
	dark = true
	features = catppuccin-frappe
	navigate = true
