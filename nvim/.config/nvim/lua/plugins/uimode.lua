-- Change neovim theme based on dark or light mode 
-- requires installation of dark-notify via homebrew
-- brew install cormacrelf/tap/dark-notify


return {
	'cormacrelf/dark-notify',
--	config = function()
--		require('dark-notify').run()
--	end,
}
